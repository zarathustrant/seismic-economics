services:
  - type: web
    name: seismic-economics
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: APP_PASSWORD
        value: seismic2024
      - key: MONGODB_URI
        sync: false
